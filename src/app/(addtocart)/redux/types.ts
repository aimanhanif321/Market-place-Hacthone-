// types.ts
export interface CartItem {
    id: string;   // Product ka ID
    name: string;  // Product ka naam
    quantity: number;  // Kitni quantity hai
    price: number;  // Product ka price
    imageUrl: string;  // Product ka image URL
  }
  